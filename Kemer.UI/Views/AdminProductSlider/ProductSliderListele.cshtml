@{
    Layout = "_AdminLayout";
}

@model List<ProductSlider>

@if (TempData["Ekleme"] != null)
{

<div class="alert success" id=mainAlertMessage>
    <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
    @TempData["Ekleme"]
</div>
}

@if (TempData["Guncellendi"] != null)
{

<div class="alert info" id=mainAlertMessage>
    <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
    @TempData["Guncellendi"]
</div>
}

@if (TempData["Silindi"] != null)
{

<div class="alert warning" id=mainAlertMessage>
    <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
    @TempData["Silindi"]
</div>
}


<div style="width:100%"><a asp-action="ProductSliderEkle" asp-controller="AdminProductSlider" asp-route-id="@ViewBag.ProductId" class="btn btn-info">Urun Resim Ekle</a></div>
<br />

<section class="content">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <!-- /.card-header -->
                <div class="card-body">
                    <table id="example2" class="table table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>Urun Resim</th>


                                <th>İŞLEMLER</th>

                            </tr>
                        </thead>

                        @foreach (var item in Model)
                        {
                        <tbody>
                            <tr>
                                <td width="100" height="100" align="center"><img src="/images/imgs/@item.PruductImage" height="100" width="100"></td>


                                <td>
                                    <a asp-action="ProductSliderSil" asp-controller="AdminProductSlider" asp-route-id="@item.Id" class="btn btn-danger" onclick="return confirm('Silmek istediginize emin misiniz');">Ürün Resim Sil</a>
                                    <a asp-action="ProductSliderGuncelle" asp-controller="AdminProductSlider" asp-route-id="@item.Id" class="btn btn-info">Ürün Resim Guncelle</a>
                                </td>

                            </tr>
                        </tbody>
                        }


                        <tfoot>
                            <tr>
                                <th>BlogCategoryName</th>


                                <th>İŞLEMLER</th>

                            </tr>
                        </tfoot>
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
            <!-- Control Sidebar -->
            <aside class="control-sidebar control-sidebar-dark">
                <!-- Control sidebar content goes here -->
            </aside>
            <!-- /.control-sidebar -->
        </div>
    </div>
</section>
