@{
    Layout = "_AdminLayout";
}

@model Referance

<div class="card card-primary">
    <div class="card-header">
        <h3 class="card-title">REFERANS EKLEME SAYFASI</h3>
    </div>
    <!-- /.card-header -->
    <!-- form start -->
    @if (ViewBag.state == "update") {<form asp-controller="AdminReferance" asp-action="ReferanceGuncelle" enctype="multipart/form-data">
                                         <div class="card-body">

                                             <div class="form-group">
                                                 <label for="exampleInputEmail1" onchange="PreviewImage();">İKON SEÇİNİZ</label>
                                                 <input type="text" asp-for="IconUrl" value="@Model.IconUrl" name="IconUrl" hidden>
                                                 <input type="file" class="form-control uploadImage" asp-for="IconUrl" id="ImageUrl" placeholder="resim seçiniz" onchange='openFile(event)'>
                                                 <div>
                                                     <img id='output' style="width : 100px;height:100px" src="/images/imgs/@Model.IconUrl"/>
                                                 </div>
                                                 <span id="error-message" class="validation-error-label"></span>
                                                 <span asp-validation-for="IconUrl" style="color:crimson"></span>
                                             </div>

                                             <div class="text-danger text-center" asp-validation-summary="ModelOnly"></div>
                                             <!-- /.card-body -->

                                             <div class="card-footer">
                                                 <button type="submit" class="btn btn-primary">REFERANS GUNCELLE</button>
                                             </div>
                                         </div>
</form> }
    else
    {
        <form asp-controller="AdminReferance" asp-action="ReferanceEkle" enctype="multipart/form-data">
                <div class="card-body">
                   
                    <div class="form-group">
                        <label for="exampleInputEmail1" onchange="PreviewImage();">İKON SEÇİNİZ</label>
                        <input type="file" class="form-control uploadImage" asp-for="IconUrl" id="IconUrl" placeholder="resim seçiniz" onchange='openFile(event)'>
                        <div>
                            <img id='output' style="width : 100px;height:100px" />
                        </div>
                        <span id="error-message" class="validation-error-label"></span>
                        <span asp-validation-for="IconUrl" style="color:crimson"></span>
                    </div>

                </div>

                <div class="text-danger text-center" asp-validation-summary="ModelOnly"></div>
                <!-- /.card-body -->

                <div class="card-footer">
                    <button type="submit" class="btn btn-primary">REFERANS EKLE</button>
                </div>
            
</form>
    }

</div>
